#!1
используется
  с-префиксом rkt: net/http-client

предоставлять отправить-получить-гипертекст

отправить-получить-гипертекст узел путь
  #:защищённый? защищённый? ложь
  #:порт порт (если защищённый? тогда 443 иначе 80)
  #:версия версия #"1.1"
  #:метод метод 'получение
  #:заголовки заголовки пустой-список
  #:данные данные ложь
  #:декодирование декодирование список('gzip 'deflate)
  =
  метод-англ = выбор метод
    (получение GET get) «GET»
    (отправка POST post) «POST»
    иначе метод
  rkt:http-sendrecv узел путь
    #:ssl? защищённый?
    #:port порт
    #:version версия
    #:method метод-англ
    #:headers заголовки
    #:data данные
    #:content-decode декодирование
