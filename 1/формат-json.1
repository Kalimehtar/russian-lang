#!1
используется
  с-префиксом rkt: json

предоставлять прочитать из-строки в-строку

прочитать
  порт текущий-порт-ввода()
  #:пусто пусто rkt:json-null()
  #:заменять-неверный-сурогат? заменять-неверный-сурогат? ложь
  =
  rkt:read-json порт
    #:null пусто
    #:replace-malformed-surrogate? заменять-неверный-сурогат?

из-строки строка
  #:пусто пусто rkt:json-null()
  #:заменять-неверный-сурогат? заменять-неверный-сурогат? ложь
  =
  rkt:string->jsexpr строка
    #:null пусто
    #:replace-malformed-surrogate? заменять-неверный-сурогат?

в-строку значение
  #:пусто пусто rkt:json-null()
  #:кодирование кодирование 'управляющие
  #:отступ отступ ложь
  =
  кодирование-англ = выбор кодирование
    (управляющие) 'control
    (все) 'all
    иначе кодирование
  rkt:jsexpr->string значение
    #:null пусто
    #:encode кодирование-англ
    #:indent отступ
