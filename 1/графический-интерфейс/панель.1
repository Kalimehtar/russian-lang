#!1
используется класс
  графический-интерфейс/объект
  графический-интерфейс/интерфейс-вместилище-окно
  графический-интерфейс/интерфейс-подокно
  графический-интерфейс/интерфейс-подобласть
  с-префиксом rkt: racket/gui/base

внутренний-панели% = сформировать-код $ #` класс rkt:panel%
  #,@начало-внутреннего
  переопределить #,@(отобразить имя-метода события-вместилища-окна)
  #,@события-вместилища-окна

панель% = сформировать-код $ #` класс* объект% (вместилище-окно<%> подокно<%>)
  базовый-объект()
  методы #,@(отобразить имя-метода $ методы-вместилища-окна ++ добавлены-методы-подобласти)
  инициализировать
    #,@(инициализация-вместилища-окна ''(центр центр))
    #,@(добавлена-инициализация-подобласти 0)
    $стиль(стиль) пустой-список
  $внутренний = объект внутренний-панели%
    получатель-событий этот
    style $стиль
    #,@внутренняя-инициализация-вместилища-окна()
    #,@добавлена-внутренняя-инициализация-подобласти
  #,@методы-вместилища-окна
  #,@добавлены-методы-подобласти
