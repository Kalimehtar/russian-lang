#!1
используется класс
предоставлять метод-выбора объект-адины внутренний-объект
предоставлять-для-синтаксиса начало-внутреннего имя-метода убрать методы-объекта

определение-синтаксического-правила метод-выбора($внутренний имя)
  функция-выбора
    () $внутренний{имя}
    (новое-значение) $внутренний{имя новое-значение}

объект-адины объект =
  объект && поле получатель-событий объект
  
внутренний-объект объект =
  объект && объект{внутренний}

при-компиляции
  методы-объекта = '
    $ внутренний() = $внутренний
  начало-внутреннего = '
    базовый-объект()
    инициализированные-поля получатель-событий    
  имя-метода элемент =
    имя = первый $ оставшиеся элемент
    список? имя ?
      первый имя
      имя
  убрать метод методы =
    если
      пустой?(методы) методы
      (имя-метода первый(методы) == метод)
        оставшиеся методы
      иначе
        пара
          первый методы
          убрать метод оставшиеся(методы)
