#!1
используется класс
  графический-интерфейс/объект
  графический-интерфейс/интерфейс-элемент-управления
  графический-интерфейс/шрифт
  с-префиксом rkt: racket/gui/base

предоставлять надпись%

внутренний-надписи% = сформировать-код $ #` класс rkt:message%
  #,@начало-внутреннего
  переопределить #,@(отобразить имя-метода события-элемента-управления)
  #,@события-элемента-управления

надпись% = сформировать-код $ #` класс* объект-графического-интерфейса% (элемент-управления<%>)
  #,@начало
  методы #,@(отобразить имя-метода методы-элемента-управления)
  инициализировать
    $заголовок(заголовок)()
    #,@инициализация-элемента-управления
    $стиль(стиль) '()
    $шрифт(шрифт) шрифт-элемента-управления
    $цвет(цвет) ложь
    $размер-по-заголовку(размер-по-заголовку) ложь
  $внутренний := объект внутренний-надписи%
    получатель-событий этот
    label $заголовок
    style $стиль
    font $шрифт{внутренний}
    color $цвет
    auto-resize $размер-по-заголовку
    #,@внутренняя-инициализация-элемента-управления
  #,@методы-элемента-управления
  размер-по-заголовку = метод-выбора $внутренний auto-resize
  цвет = функция-выбора
    () $внутренний{get-color}
    (значение) $внутренний{set-color значение}
