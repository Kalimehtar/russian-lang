#!1
системная
используется базовая/функция
  с-префиксом rkt: racket
предоставлять путь? строка->путь путь->строка построить-путь

путь? объект =
  rkt:path? объект

строка->путь строка =
  rkt:string->path строка

путь->строка путь =
  rkt:path->string путь

построить-путь база . элементы =
  применить rkt:build-path база элементы
