#!1
системная
используется
  с-префиксом rkt: racket/base
  с-префиксом mt: racket/match
предоставлять список список? пара пара? пустой? пустой-список :

mt:define-match-expander список
  функция (выражение)
    выбор-синтаксиса выражение ()
      (_ образец ...)
         #'(list образец ...)
  функция (выражение)
    выбор-синтаксиса выражение ()
      (_ образец ...)
         #'(rkt:list образец ...)

mt:define-match-expander пара
  функция (выражение)
    выбор-синтаксиса выражение ()
      (_ образец ...)
         #'(cons образец ...)
  функция (выражение)
    выбор-синтаксиса выражение ()
      (_ образец ...)
         #'(rkt:cons образец ...)

а : б =
  пара а б

список? а =
  rkt:list? а

пара? а =
  rkt:pair? а

пустой? список =
 rkt:null? список

пустой-список = rkt:null
