#!1
системная
используется
  с-префиксом rkt: racket/base
предоставлять выбор

(определение-синтаксиса (выбор синтаксис)
  (выбор-синтаксиса синтаксис (иначе)
    [(_ ВЫРАЖЕНИЕ ((ЗНАЧЕНИЕ ...) ...) ... (иначе ТЕЛО-ИНАЧЕ ...))     
     #'(rkt:case ВЫРАЖЕНИЕ ((ЗНАЧЕНИЕ ...) ...) ... (rkt:else ТЕЛО-ИНАЧЕ ...))]
    [(_ А ...) #'(rkt:case А ...)]))