#!1
системная
используется
  с-префиксом rkt: racket/base
  1/syn
предоставлять функция? функция применить пусто пусто?

синоним rkt:lambda функция

функция? объект =
  rkt:procedure? объект

применить = rkt:make-keyword-procedure
  функция (имена-ключей значения-ключей ф . остальное)
    rkt:keyword-apply ф имена-ключей значения-ключей $ rkt:apply rkt:list* остальное
  функция (ф . аргументы)
    rkt:apply ф $ rkt:apply rkt:list* аргументы

пусто = (rkt:void)

пусто? объект =
  rkt:void? объект
