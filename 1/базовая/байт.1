#!1
системная
используется
  с-префиксом rkt: racket/base
предоставлять байт? байты? элемент-байтов установить-элемент-байтов! новые-байты длина-байтов \
  байты->строка байты->строка/местные

байт? объект =
  rkt:byte? объект

байты? объект =
  rkt:bytes? объект

элемент-байтов байты позиция =
  rkt:bytes-ref байты позиция

установить-элемент-байтов! байты позиция значение =
  rkt:bytes-set! байты позиция значение

новые-байты количество (значение 0) =
  rkt:make-bytes количество значение

длина-байтов байты =
  rkt:bytes-length байты

байты->строка байты (литера-ошибки ложь) (начало 0) (конец длина-байтов(байты)) =
  rkt:bytes->string/utf-8 байты литера-ошибки начало конец

байты->строка/местные байты (литера-ошибки ложь) (начало 0) (конец длина-байтов(байты)) =
  rkt:bytes->string/locale байты литера-ошибки начало конец
