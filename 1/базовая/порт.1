#!1
системная
используется
  с-префиксом rkt: racket/base
  базовая/управление
  базовая/синтаксис
  базовая/функция
предоставлять читая-файл записывая-файл есть-файл? вывести вывести/перенос записать записать/перенос \
  открыть-запись-в-строку получить-записанную-строку записать-строку текущее-место

читая-файл имя обработка
  #:режим режим 'двоичный
  =
  mode = выбор режим
    (двоичный) 'binary
    (текстовый) 'text
  вывести mode
  rkt:call-with-input-file имя обработка #:mode mode

записывая-файл имя обработка
  #:режим режим 'двоичный
  #:если-существует существует 'ошибка
  #:права права #o666
  =
  mode = выбор режим
    (двоичный) 'binary
    (текстовый) 'text
  exists = выбор существует
    (ошибка) 'error
    (добавить) 'append
    (обновить) 'update
    (заменить) 'replace
    (обрезать) 'truncate
    (обрезать/заменить) 'truncate/replace
  rkt:call-with-output-file имя обработка
    #:mode mode
    #:exists exists
    #:permissions права

есть-файл? файл =
  rkt:file-exists? файл

вывести что
  порт rkt:current-output-port()
  =
  rkt:display что порт
   
вывести/перенос что
  порт rkt:current-output-port()
  =
  rkt:displayln что порт

записать что
  порт rkt:current-output-port()
  =
  rkt:write что порт

записать/перенос что
  порт rkt:current-output-port()
  =
  rkt:writeln что порт

открыть-запись-в-строку (имя 'строка) =
  rkt:open-output-string имя

получить-записанную-строку порт =
  rkt:get-output-string порт

записать-строку строка
  порт rkt:current-output-port()
  =
  rkt:write-string строка порт

текущее-место = функция-выбора
  ()
    rkt:current-locale()
  (место)
    rkt:current-locale место
