#!1
системная
используется
  с-префиксом rkt: racket/base
  с-префиксом mt: racket/match
используется-для-синтаксиса
  базовая/синтаксис
  базовая/функция
предоставлять массив массив? длина-массива

mt:define-match-expander массив
  функция (выражение)
    выбор-синтаксиса выражение ()
      (_ образец ...)
         #'(vector образец ...)
  функция (выражение)
    выбор-синтаксиса выражение ()
      (_ образец ...)
         #'(rkt:vector образец ...)
      _ #'(rkt:procedure-rename rkt:vector 'массив)

массив? м =
  rkt:vector? м

длина-массива м =
  rkt:vector-length м
