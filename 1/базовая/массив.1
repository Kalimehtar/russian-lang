#!1
системная
используется
  с-префиксом rkt: racket/base
  с-префиксом mt: racket/match
используется-для-синтаксиса
  базовая/синтаксис
  базовая/функция
предоставлять массив массив? длина-массива массив->список список->массив новый-массив \
  элемент-массива установить-элемент-массива!

mt:define-match-expander массив
  функция (выражение)
    выбор-синтаксиса выражение ()
      (_ образец ...)
         #'(vector образец ...)
  функция (выражение)
    выбор-синтаксиса выражение ()
      (_ образец ...)
         #'(rkt:vector образец ...)
      _ #'(rkt:procedure-rename rkt:vector 'массив)

массив? массив =
  rkt:vector? массив

длина-массива массив =
  rkt:vector-length массив

массив->список массив =
  rkt:vector->list массив

список->массив список =
  rkt:list->vector список

новый-массив длина (значение 0) =
  rkt:make-vector длина значение
  
элемент-массива массив позиция =
  rkt:vector-ref массив позиция

установить-элемент-массива! массив позиция значение =
  rkt:vector-set! массив позиция значение
