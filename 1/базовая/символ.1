#!1
системная
используется
  с-префиксом rkt: racket/base
предоставлять символ? символ->строка строка->символ

символ? объект =
  rkt:symbol? объект

символ->строка символ =
  rkt:symbol->string символ

строка->символ строка =
  rkt:string->symbol строка
