#!1
используется 
  с-префиксом rkt: racket/port

предоставлять порт->байты порт->строка порт->строки

порт->байты порт =
  rkt:port->bytes порт
  
порт->строка порт =
  rkt:port->string порт
  
порт->строки порт =
  rkt:port->lines порт
